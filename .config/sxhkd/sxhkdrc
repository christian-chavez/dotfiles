
# wm independent hotkeys
#

# terminal emulator
super + Return
	alacritty

## program launcher
super + @space
	rofi -no-lazy-grab -show combi -combi-modi drun,menu:rofi-power-menu -normal-window
	# rofi -no-lazy-grab -drun-reload-desktop-cache -combi-modi drun,menu:rofi-power-menu -show combi
        # dmenu_run -f -b -fn 'Iosevka-10.5' -nb '#252a34' -sf '#1e1e1e' -sb '#88c0d0' -nf '#D3D3D3' 
	# dmenu_run
	# $HOME/.dmenu_run_history -f -b -fn 'Iosevka-10.5' -nb '#252a34' -sf '#1e1e1e' -sb '#88c0d0' -nf '#D3D3D3'
	# rofi -drun-use-desktop-cache -combi-modi drun,menu:rofi-power-menu -show combi

# Clipboard greenclip
super + a
	rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}' -normal-window 

# google search from rofi
super + d
	$HOME/Documents/scripts/rofi-google.sh
	# rofi -dmenu | xargs -I{} xdg-open https://www.google.de/search?q={}
    # bash -c 'q=$(rofi -dmenu -p "Google" -lines 1 -theme-str "listview { lines: 0; fixed-height: 0; }" </dev/null); [ -n "$q" ] && firefox "https://www.google.com/search?q=$(printf %s "$q" | sed "s/[[:space:]]\+/+/g")"'



# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd

#
# bspwm hotkeys
#

# quit/restart bspwm
super + alt + {q,r}
	bspc {quit,wm -r}

# close and kill
super + {_,shift + }w
	bspc node -{c,k}

# alternate between the tiled and monocle layout
super + m
	bspc desktop -l next

# send the newest marked node to the newest preselected node
super + y
	bspc node newest.marked.local -n newest.!automatic.local

# swap the current node and the biggest window
super + g
	bspc node -s biggest.window

#
# state/flags
#

# set the window state
super + {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# set the node flags
super + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

#
# focus/swap
#

# focus the node in the given direction
super + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# focus the node for the given path jump
super + {p,b,comma,period}
	bspc node -f @{parent,brother,first,second}

# focus the next/previous window in the current desktop
super + {_,shift + }c
	bspc node -f {next,prev}.local.!hidden.window

# focus the next/previous desktop in the current monitor
super + bracket{left,right}
	bspc desktop -f {prev,next}.local

# focus the last node/desktop
super + {grave,Tab}
	bspc {node,desktop} -f last

# focus the older or newer node in the focus history
super + {o,i}
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}' --follow

#
# preselect
#

# preselect the direction
super + ctrl + {h,j,k,l}
	bspc node -p {west,south,north,east}

# preselect the ratio
super + ctrl + {1-9}
	bspc node -o 0.{1-9}

# cancel the preselection for the focused node
super + ctrl + space
	bspc node -p cancel

# cancel the preselection for the focused desktop
super + ctrl + shift + space
	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel


#
# move/resize
#

# expand a window by moving one of its side outward
super + alt + {h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
super + alt + shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move a floating window
super + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}

# change orientation (vertical -> horizontal)
super + shift + e
	bspc node '@parent' -R 90

# Swap focused window with neighbor
super + x
	bspc node -s last.local; \
	bspc node -f last.local

# Screnshot tool
Print
	$HOME/myscripts/flameshot-focus.sh
	# flameshot gui 

# Capture selection and save inmediately
shift + Print
	flameshot gui -p $HOME/Pictures/Screenshots

# Capture entire screen and save inmediately
super + Print
	flameshot full --region all -p $HOME/Pictures/Screenshots

# lf file manager
# super + e
	# alacritty -e 'lf'
	# alacritty -e lf

# thunar file manager
super + e
	Thunar

# Volumen control
XF86AudioRaiseVolume
	wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l 1
	# pactl set-sink-volume 0 +2%; pactl set-sink-mute 0 false
	
XF86AudioLowerVolume
	wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
	# pactl set-sink-volume 0 -2%; pactl set-sink-mute 0 false
	
XF86AudioMute
	wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
    	# pactl set-sink-mute 0 toggle

# brightness control down
XF86MonBrightnessDown
	brillo -u 100000 -U 5
	# brillo -U 5
	# brightnessctl s 10{+,-} 

XF86MonBrightnessUp
	brillo -u 100000 -A 5

# set max brightness
super + alt + m
	brillo -u 150000 -S 100
	# brightnessctl s 255 

# set min brightness
super + alt + n
	brillo -u 150000 -S 0
	# brightnessctl s 0

# play/pause
XF86AudioPlay
	playerctl play-pause

# Skip to the next track
XF86AudioNext
	playerctl next

# Skip to the previous track
XF86AudioPrev
	playerctl previous

# Shortcut for date
# super + d
	# sleep 0.30 && setxkbmap latam && xdotool type --delay=10 "$(date "+%Y-%m-%d %a %H:%M:%S")"

# Catfish
super + shift + @space
	$HOME/Documents/scripts/rofi-files-google-drive.sh
	# catfish --start /mnt/Mis\ archivos/Google\ Drive
	# catfish --start /mnt/Mis\ archivos/Google\ Drive/Libros

# Open obsidian
super + shift + o
	$HOME/Documents/Obsidian-1.9.14.AppImage --no-sandbox
	# $HOME/Documents/Obsidian-1.5.3.AppImage

# Open obsidian
super + shift + m
	$HOME/Documents/Mathpix.AppImage

# Wacom support
super + shift + x
	$HOME/Documents/xsetwacom-new.sh

super + shift + alt + x
	$HOME/Documents/xsetwacom-new-laptop.sh


# start redshift
super + shift + r
	redshift -O 4000

# Schedule
# super + shift + s
	# feh "/mnt/Mis archivos/Google Drive/10 Décimo Semestre/Otros/Schedule/_temp/schedule.png"

# Calendario académico
# super + shift + d
	# zathura --mode fullscreen "/mnt/Mis archivos/Google Drive/9 Noveno Semestre/Otros/Calendario-Iy-II-PERIODO-2024-Carrera.pdf"

# Launch LibreWolf profile
# super + shift + f
# 	librewolf -p MathClub

# Launch browser
super + @b
	firefox
	# librewolf

# Multi shorcuts
super + u ; {i,c,m,e,f,n,s,u,U,M,k,d,j,p}
	$HOME/Documents/scripts/type_info.sh {i,c,m,e,f,n,s,u,U,M,k,d,j,p}

# Suspend
super + @l
	systemctl suspend-then-hibernate
#	systemctl suspend

super + shift + @l
	dm-tool lock && systemctl suspend

# alt + Tab
# 	bspc node -s last.local; \
# 	bspc node -f last.local
	# bspc node -f last
	# rofi -show window
    # $HOME/Documents/scripts/rofi-alt-tab
	# bspc node -f next.local
# super + Tab
# 	rofi -show window

super + shift + a
	$HOME/Documents/scripts/rofi-files.sh

# super + alt + @c
# 	xdotool type ç
	# mpv av://v4l2:/dev/video0 --profile=low-latency --untimed --no-correct-pts > /dev/null 2>&1 &

super + shift + i
	$HOME/.config/bspwm/scripts/winhide

# super + ctrl + shift + i
# 	w=$(bspc query -N -n .hidden | while read i; \ 
# 	do echo $(xprop -id $i | grep WM_CLASS | sed -n 's/.*"\(.*\)",.*/\1/p'); done | rofi -dmenu);
	# if [ -n "$w" ]; then \
	# 	w=$(echo $w | cut -d ' ' -s -f 1); \
	# 	bspc node "$w" -g hidden=off; \
	# 	bspc node $w.fullscreen -t tiled; \
	# 	bspc node $w -l normal; \
	# 	bspc node -f $w; \
	# fi

# recent list of pdfs with rofi, all time history
super + shift + @p
    $HOME/Documents/scripts/zathura-recent-files-rofi list

# # currently opened pdfs
# super + @r
#     $HOME/Documents/scripts/pdf-switch-rofi


super + y
    $HOME/Documents/scripts/toggle-polytabs.sh

